# 寅子鱼吧爬取 App · 完全使用指南

> 这是一份给普通用户看的上手文档。开发者请看同级目录下的 `README.md`。

---

## ⚠️ 使用前请先看（必读）

- 本 App 仅用于**个人学习与收藏**。请不要把下载的图片或视频：
  - 发到公开平台（微博、贴吧、抖音、B站等）
  - 做二次剪辑用于牟利
  - 打包贩卖或在粉丝群之外大量分发
- **版权归原作者（寅子及各帖子作者）所有**。你下载的内容请自己看完就好。
- 斗鱼平台有反爬风控，建议**不要一次下载几百个视频**，否则 IP 会被限流。

---

## 一、它能做什么？

自动读取**斗鱼寅子鱼吧**（主播寅子的粉丝社区）里的帖子，把帖子里的图片和 mp4 视频一键保存到你手机相册。支持：

| 功能 | 说明 |
| --- | --- |
| 拉取帖子 | 默认寅子鱼吧，可切换成任意其他主播鱼吧 |
| 解析图片 | jpg / png / webp / gif，自动识别 |
| 解析视频 | mp4 直链自动下载（鱼吧内嵌的短 mp4） |
| 单帖保存 | 点每个帖子右下角的「保存」 |
| 整页保存 | 点右下角「下载本页」一键拉当前列表 |
| 后台下载 | 切到微信/桌面也不会中断，有通知栏提示进度 |
| 存到相册 | 图片在相册 `Pictures/YinziCrawler/Images`；视频在 `Movies/YinziCrawler/Videos` |

## 二、装到手机上

### 方式 A：直接装 APK（最简单）

1. 把编译好的 `app-debug.apk` 传到手机（微信文件传输助手 / USB 拷贝都可以）
2. 手机上打开文件管理器，找到这个 apk 点开安装
3. 如果提示「未知来源应用」，点「允许」即可
4. 桌面出现 🐠「寅子鱼吧爬取」图标 → 安装完成

> APK 在工程里的位置：`YinziCrawler/app/build/outputs/apk/debug/app-debug.apk`

### 方式 B：开发者自己编译

Android Studio 打开项目目录点 Run 即可，详见 `README.md`。

## 三、第一次打开必须做的事：填 Cookie

鱼吧是半公开社区，**不登录会只抓到一小部分**。想要完整内容必须填 Cookie。

### 3.1 在电脑浏览器里抓 Cookie

1. 电脑打开 **Chrome / Edge 浏览器**，访问 <https://yuba.douyu.com>
2. 右上角点「登录」，用你的斗鱼账号登录（扫码、手机号都行）
3. 登录成功后，在地址栏输入寅子鱼吧地址并打开：
   ```
   https://yuba.douyu.com/discussion/561/posts
   ```
4. 键盘按 **F12**，会弹出开发者工具面板 → 点顶部的 **Network（网络）** 标签
5. 浏览器地址栏左边点一下 🔄 **刷新按钮**
6. 左侧 Network 列表里会出现一堆请求 → 点**最上面第一条**（名字是 `561` 或 `posts` 那条）
7. 右侧会出现 Headers → 往下翻找到「**请求标头**」或「**Request Headers**」→ 找到 `Cookie:` 这一行
8. 把 `Cookie:` **冒号后面的整串文本**完整复制（Ctrl+A 选中 → Ctrl+C）
   - 看起来大概像这样（几十到几百字符）：
     ```
     dy_auth=xxx; acf_did=xxx; acf_stk=xxx; acf_uid=xxx; ...
     ```

### 3.2 填进 App

1. 打开「寅子鱼吧爬取」→ 右上角点 **⚙ 设置**
2. 把刚才复制的 Cookie 粘贴到第二个输入框
3. 第一个输入框「group_id」**默认 561**，不用改（561 就是寅子）
4. 点「保存」，App 会自动刷新列表

> 🔒 **隐私说明**：Cookie 只保存在你手机的 App 私有目录里，不会上传到任何服务器。  
> ⏱️ Cookie 一般 **几天～几周会过期**（斗鱼服务端决定）。哪天突然只出空白、加载失败，重新按步骤抓一次 Cookie 粘贴进来就行。

## 四、界面详解

```
┌──────────────────────────────────────┐
│  寅子鱼吧爬取          ⚙ 设置       │  ← 顶部栏
│  group_id: 561                      │
├──────────────────────────────────────┤
│  ↓ 下拉刷新（Swipe 动作）            │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 🟦 寅子     2025-10-27  保存 ▶  │  │  ← 帖子卡片
│  │ 显摆一下 全喀什唯一一把…         │  │
│  │ ┌───┐ ┌───┐ ┌───┐             │  │
│  │ │🖼️│ │🖼️│ │▶️ │             │  │  ← 媒体网格
│  │ └───┘ └───┘ └───┘             │  │
│  │ 3 图 · 1 视频                  │  │
│  └────────────────────────────────┘  │
│          …（更多卡片）                │
│                                      │
│                  ⬇ 下载本页 📥      │  ← 浮动按钮
└──────────────────────────────────────┘
```

### 4.1 每个位置的功能

| 区域 | 操作 | 效果 |
| --- | --- | --- |
| 右上角「⚙ 设置」 | 点击 | 弹窗改 group_id / 粘贴 Cookie |
| 帖子卡片「保存」按钮 | 点击 | 把该帖里所有图片和视频加入下载队列 |
| 图片缩略图（▶️ 标签的是视频） | 点击 | 图片：**预览大图**；视频：**直接下载** |
| 右下角「下载本页」 | 点击 | 把当前列表里所有帖子的全部媒体一起排队下 |
| 页面顶部下拉（手指往下滑） | 手势 | 刷新第一页帖子 |
| 滑到接近底部 | 自动 | 触发「加载更多」翻下一页 |

### 4.2 下载怎么知道进度？

- App 顶部通知栏会出现「🐠 正在下载… (5/27)」这种常驻通知
- 每个媒体项右上角 ✅ 小绿勾表示已经下载过
- 下载的内容**直接进相册**：打开系统「图库/照片 App」就能看到

## 五、常用 fish bar group_id 速查

想爬别的主播，把以下数字填进设置里的「group_id」即可：

| group_id | 主播 / 鱼吧 |
| --- | --- |
| **561** | **寅子（默认）** |
| 74 | 斗鱼官方 |
| 515 | 主机游戏区综合 |
| 496557 | 小一熟了吗 |
| 1561201 | Big茄子 |
| 1481383 | 嘉瑞baby |

> 找某个主播的 group_id 方法：浏览器打开 ta 的鱼吧，看地址栏最后一段数字就是。例如 `yuba.douyu.com/discussion/1561201/posts` → group_id = `1561201`。

## 六、常见问题（FAQ）

### Q1：打开 App 只有空白，一个帖子也不显示

**最常见原因**：
1. 你没填 Cookie → 按「第三节」抓 Cookie 贴进去
2. 网络不通，或开了代理/VPN 连不上斗鱼 → 关掉代理重试
3. Cookie 过期了 → 重新在浏览器抓一遍

排查：先在手机浏览器里直接打开 <https://yuba.douyu.com/discussion/561/posts>，如果能正常看，就是 App 里的 Cookie 有问题。

### Q2：能看到帖子，但没图片/视频

90% 的概率是**该帖子的媒体是登录态可见**，同样是 Cookie 没填对。  
另外少数情况是斗鱼把图链换成了特殊格式、我们解析器暂时没覆盖 → 下拉刷新一下可能就好。

### Q3：视频下载下来只有几十字节 / 0 KB 损坏

说明抓到的不是 mp4 直链，而是 m3u8 切片流地址。当前版本**暂不支持 m3u8 合并下载**。这种视频可以：
- 在帖子里先记下标题，自己搜直播回放网站
- 或者等后续版本加 m3u8 合并功能

### Q4：相册里找不到下载的图/视频

不同手机相册 App 显示位置不一样：
- **华为/小米/OPPO**：在「相册 → 其他相册 → YinziCrawler/Images」或「YinziCrawler/Videos」
- **一加/原生**：直接在相册时间线里能看到

如果还是找不到，用系统自带的「文件/我的文件」App，依次定位到：
```
内部存储 → Pictures → YinziCrawler → Images
内部存储 → Movies   → YinziCrawler → Videos
```

### Q5：提示「没有存储权限，无法保存文件」

Android 13 及以上第一次打开会弹 **「允许读取媒体图片/视频」** 权限请求：
- 点「允许」就好
- 如果之前拒绝了：系统设置 → 应用 → 寅子鱼吧爬取 → 权限 → 把「照片和视频」勾上

### Q6：下载了一半，切到别的 App 就停了

理论上不会（我们用了前台服务，通知栏那个常驻图标就是它活着的证明）。  
如果真被某些国产系统「电池优化」杀了：

- 华为：手机管家 → 应用启动管理 → 找到 App → 手动管理 → 允许后台活动
- 小米：安全中心 → 电量 → 应用省电策略 → 无限制
- OPPO：设置 → 电池 → 应用耗电管理 → 允许后台活动

### Q7：突然所有请求都 403 / 无响应

多半是你下载太多被斗鱼风控了。两种办法：
1. 关闭 App，**等 10～20 分钟**再打开
2. 去设置里把 Cookie 换一份（浏览器重新登录换一个新的 Cookie 粘贴）

**建议**：一次性下 20～30 个就歇一会儿，别贪心。

## 七、关于接口和未来

斗鱼鱼吧的接口是**非公开**的，官方偶尔会改字段名、改路径。为了抗变化，本 App 做了三级容错（JSON → Next.js 内嵌 JSON → DOM 解析），解析器用「语义 key 匹配」而不是硬编码字段名，一般的小改动不会挂。

如果**某天真的失效**（列表空、全是错误），你可以：
1. 先按前面 FAQ 检查 Cookie 是否过期
2. 换个浏览器再抓 Cookie 贴一遍
3. 如果还不行 → 让开发者对照 `README.md` 里「接口与容错说明」更新下 `YubaApi.kt` 的请求路径

## 八、技术反馈

遇到崩溃、功能缺失、或想加功能（比如关键词筛选、只下视频不下图、m3u8 合并、批量导出等），可以和作者提。代码全在 `YinziCrawler/` 目录下，可以随便改。

---

> 📌 **再重申一遍**：这个 App 只是方便个人收藏寅子以及其他主播鱼吧里的图片视频，请尊重作者版权和平台规则，合理使用。
